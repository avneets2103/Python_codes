#modules are used in python #we use import keyword for using these modules
#module can be either external: download using pip or internal: already present in the python
#you can use pip for downloading external modules
#we can just type python in terminal to use it as a calculator #we generally use terminal to check some few lines of codes
#Multiline comment by '''''' or multiple line string by ''''''
#REPL refers to Read Evaluated Print Loop which is a loop which runs until uh type exit()

import os 
print("hello world") 
print('''Hello
world''')

